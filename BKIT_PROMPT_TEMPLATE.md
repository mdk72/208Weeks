# 🚀 BKIT(Backtest Kit) 모듈형 프로젝트 요청 템플릿

다음 프로젝트를 시작할 때 이 내용을 복사해서 AI에게 전달하세요. AI가 처음부터 가장 깔끔하고 전문적인 구조로 코딩을 시작할 것입니다.

---

## 📋 AI에게 전달할 프롬프트 (복사해서 사용하세요)

"새로운 **[여기에 프로젝트 이름 입력]** 프로그램을 개발하고 싶어. 
유지보수와 확장성을 고려하여 **'BKIT(Backtest Kit) 모듈형 구조'**로 전체 프로젝트 뼈대를 설계해줘.

### 1. 프로젝트 핵심 구조 (관심사 분리)
프로젝트는 다음과 같은 폴더 및 파일 구조를 가져야 해:
- `src/ui/`: Streamlit이나 다른 라이브러리를 사용한 UI 표시 로직
- `src/engine/`: 핵심 계산 및 시뮬레이션 알고리즘 (UI와 완전히 독립될 것)
- `src/strategies/`: 개별 전략/규칙들을 정의하는 모듈 (나중에 교체 가능하게 설계)
- `src/data/`: API 호출, DB 처리, 데이터 로딩 유틸리티
- `config/`: JSON 또는 YAML 형식의 설정 파일 저장 폴더
- `main.py`: 프로그램 전체를 실행하는 단일 진입점 파일

### 2. 기술적 요구사항
- **전략 패턴 적용**: 새로운 전략을 추가할 때 기존 엔진 코드를 수정하지 않고 파일만 추가하면 되도록 설계해줘.
- **의존성 최소화**: 핵심 로직(`engine`)이 UI 라이브러리에 의존하지 않게 해서, 나중에 웹이 아닌 터미널에서도 돌아갈 수 있게 해줘.
- **설정 중앙화**: 하드코딩을 피하고 모든 변수(API 키, 파라미터 등)는 `config/` 내 파일에서 관리해줘.
- **백업 자동화**: 중요한 작업을 시작하기 전에는 항상 기존 코드를 `backup/` 폴더에 복사하는 기능을 제안해줘.

### 3. 개발 순서
먼저 전체적인 폴더 구조와 인터페이스 정의를 포함한 **'구현 계획(Implementation Plan)'**을 문서로 작성해서 보여주고, 내가 승인하면 코딩을 시작해줘."

---

## 💡 팁
- **[여기에 프로젝트 이름 입력]** 부분에는 예를 들어 "코인 자동매매", "재무제표 스크리너" 등을 적으시면 됩니다.
- AI가 계획을 세울 때 **"더 전문적인 설계 패턴(예: Singleton, Factory)도 적용해줘"**라고 덧붙이면 더 고급 코드를 얻을 수 있습니다.
